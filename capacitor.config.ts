const config = {
  appId: "com.moniclaundry.pos",
  appName: "Monic Laundry POS",
  webDir: "public",
  server: {
    url: "https://moniclaundry.vercel.app", // Vercel এর URL
    cleartext: true,
  },
};

export default config;
